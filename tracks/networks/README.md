# Трек: компьютерные сети

Самый ранний трек — формат ещё не зафиксирован.

| Файл | Содержание |
|---|---|
| [`junior_qa.md`](junior_qa.md) | 10 вопросов уровня Junior с подробными ответами (IP, маски, TCP/UDP, DNS, HTTP) |

Когда трек дорастёт до систематического курса, имеет смысл перейти на структуру
разделов по образцу [`../shad/`](../shad/) — тулинг в `tools/` от неё не зависит
только в части `--section`, список разделов задаётся в `tools/generate_lecture.py`.
