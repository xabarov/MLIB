# PROJECT BOOTSTRAP — <НАЗВАНИЕ ПРОЕКТА>

> `init()` проекта. Агент читает этот файл первым и получает «операционную систему» проекта.
> Заполняется один раз при запуске; дальше живёт в CLAUDE.md. Удаляй неприменимые секции.
>
> Концепция и обоснование структуры — в `new_spiral.md` (раздел 10).

---

## 1. Суть проекта (1 абзац)

<Что это, для кого, какую боль решает. Без воды — агент опирается на это при принятии решений.>

## 2. Стек

- **Backend:** <язык, фреймворк, версия>
- **Frontend:** <…>
- **Хранилище:** <БД, кэш>
- **LLM-провайдер:** <openrouter / anthropic / …> · модель по умолчанию: <…>
- **Бюджет:** <лимит $/мес, rate limits — то, что нельзя превышать>

## 3. Запрещено агенту (DO NOT) — pre-condition assertions

- [ ] не коммитить `.env`, секреты, ключи
- [ ] не менять <дизайн-систему / схему БД / публичный API> без явного ОК
- [ ] не добавлять моки в тесты, которые ходят в реальную БД
- [ ] <свои запреты>

## 4. Разрешено без подтверждения

- [ ] читать любые файлы проекта
- [ ] запускать тесты и линтеры
- [ ] <безопасные операции, на которые не нужно спрашивать>

## 5. Структура roadmap / backlog

- **Где лежит:** `<docs/backlog/roadmap.md>`
- **Формат задачи:** `[Priority P0..P3] [Status] [depends_on: …]` + описание
- **Правило роста:** если roadmap раздувается > <N> строк — суммаризировать по правилу <…>
- **Правило открытий:** нашёл новую проблему по ходу работы — пиши сюда же, не теряй

## 6. DoD по умолчанию (post-condition)

- [ ] тесты зелёные: `<команда>`
- [ ] линтер/типы чисты: `<команда>`
- [ ] документация / changelog обновлены
- [ ] нет регрессий в <ключевых бенчмарках / метриках>
- [ ] <проектные критерии «сделано»>

(Для длинных циклов — выделить отдельный `*_cycle_dod.md` с тиерами Required / Quality / Docs.)

## 7. UI/UX правила (если есть фронт)

- Палитра: <…>
- Запрещённые паттерны: <…>
- Источник истины по дизайну: `<путь>`

## 8. Ключевые команды

```bash
# тесты
<...>
# запуск dev-сервера
<...>
# сборка / деплой
<...>
# бенчмарк / eval
<...>
```

## 9. Ресурсные лимиты (модель сложности агента)

- Контекст: держать рабочее окно < <N> токенов
- Стоимость: не превышать <$> за прогон / сессию
- Параллелизм: до <N> одновременных агентов
- Тайм-ауты: <agent timeout, LLM timeout>

## 10. Инварианты (что НЕ должно меняться в течение цикла)

- версия рантайма: <Python 3.12 / Node 20 / …>
- дизайн-палитра: <…>
- публичные контракты / схемы: <…>
- <иные инварианты — нарушение = стоп и эскалация к человеку>

## 11. Карта памяти проекта

| Уровень | Файл | Что там |
|---|---|---|
| Cross-project | `~/.claude/.../MEMORY.md` | паттерны, предпочтения, инсайты |
| Конфиг проекта | `CLAUDE.md` | стек, DoD, запреты, команды |
| Состояние работы | `<roadmap.md>`, `<dod.md>` | задачи, статус, контракт цикла |
| Стартовый снимок | этот файл | заполняется один раз при init |

---

*Шаблон из `new_spiral.md`. Скопируй в новый проект, заполни `<…>`, удали лишнее.*
